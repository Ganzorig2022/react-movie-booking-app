// export const userPhoneIsAvailable = (userDataObj) => {
//   const loggedUserDataArr = userDataObj;
//   const result = loggedUserDataArr.find((user) => user.phone === enteredPhone);
//   if (result) return true;
//   if (!result) return false;
// };
